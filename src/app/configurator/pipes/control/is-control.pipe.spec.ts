import { IsControlPipe } from './is-control.pipe';

describe('IsControlPipe', () => {
  it('create an instance', () => {
    const pipe = new IsControlPipe();
    expect(pipe).toBeTruthy();
  });
});
