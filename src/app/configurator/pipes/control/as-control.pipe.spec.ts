import { AsControlPipe } from './as-control.pipe';

describe('AsControlPipe', () => {
  it('create an instance', () => {
    const pipe = new AsControlPipe();
    expect(pipe).toBeTruthy();
  });
});
